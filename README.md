# MongoDB Kubernetes Cluster

This project creates a stateful minikube cluster with at-rest encryption for secrets and configmaps, with a dashboard and volumesnapshot capabilities

## Installation

-Install minikube
-Download the project
-Run ./deploy.sh to start the cluster
